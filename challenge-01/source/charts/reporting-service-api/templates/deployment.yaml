apiVersion: apps/v1
kind: Deployment
metadata:
  name: reporting-service-api
  namespace: {{.Release.Namespace}}
  labels:
    app: reporting-service-api
spec:
  replicas: {{.Values.deployment.replicas}}
  selector:
    matchLabels:
      app: reporting-service-api
  template:
    metadata:
      labels:
        app: reporting-service-api
    spec:
      containers:
        - name: reporting-service-api
          image: "{{.Values.deployment.image.registry}}/challenge-01/reporting-service-api:{{.Values.deployment.image.tag}}"
          ports:
            - containerPort: {{.Values.deployment.containerPort}}
          imagePullPolicy: {{.Values.deployment.image.pullPolicy}}
          volumeMounts:
            {{- toYaml .Values.deployment.volumeMounts | nindent 12 }}
    {{- with .Values.deployment.volumes }}
      volumes:
        {{- toYaml . | nindent 8 }}
    {{- end }}